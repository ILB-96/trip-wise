/** @type {import('next').NextConfig}*/
const nextConfig = {
  images: {
    domains: [
      "upload.wikimedia.org",
      "en.wikipedia.org",
      "www.telegraph.co.uk",
    ],
  },
};

export default nextConfig;
